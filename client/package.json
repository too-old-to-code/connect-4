{
  "name": "connect-four",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "browserify src/app.js -o dist/bundle.js && cp index.html dist/index.html && cp style.css dist/style.css",
    "dev": "npm run build && open index.html && nodemon ../server/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.2.3"
  },
  "dependencies": {
    "socket.io-client": "^2.2.0"
  }
}
